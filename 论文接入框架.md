 当前做到哪里了
  主框架已经基本收稳，当前回归是 77 passed。现在已经正式进入三篇论文接入阶段，不是准备阶段了。

  三条线当前状态：

  - EAI 进度最高，已经接上真实 prompt cache、真实模型、真实官方 evaluator 调用。framework 模式已经能在简单 case 上把 EAI prompt 送进 OurAgent 再翻回 EAI 动作格式。
  - DELTA 已经接上真实任务定义和 scene graph，能把四个 domain 的任务装成 BenchmarkCase，也有第一版 one-shot runner。
  - ReAcTree/ZSP 已经接上真实 WAH 数据，能把 init_graph 装成场景，走第一版 one-shot runner，并有 GSR/SSR 聚合。

  benchmark 总体思路
  现在这套 benchmark 不是“每篇论文各写一坨独立代码”，而是统一成一条总链：

  1. adapter.py
     把论文原始数据转成 BenchmarkCase
  2. config.py
     给这条 benchmark 提供路径、默认参数、模型配置
  3. framework_runner.py
     把 benchmark 自己的 scene 临时注入主框架，关闭 reflection/ACE，只跑 understanding/planning/sandbox/re-trac
  4. runner.py
     控制到底是：
      - 直接 prompt 模型输出论文格式动作
      - 还是先走 OurAgent 框架，再翻译回论文 evaluator 能吃的动作
  5. metrics.py
     调论文自己的 evaluator，或者按论文口径聚合结果

  通俗讲：

  - adapter 负责“把论文世界翻译成我们的输入”
  - runner 负责“怎么跑”
  - metrics 负责“怎么算分”

  ———

  ## 公共 benchmark 文件

  ### /data/zmy/OurAgent/benchmark/base.py

  作用：定义 benchmark 的通用数据结构和执行器。

  关键对象：

  - BenchmarkCase
    一条样本，包含 case_id / input / reference / metadata
  - BenchmarkResult
    跑完后的结果
  - BenchmarkRunner.run()
    遍历 case，调用 agent_fn
  - BenchmarkRunner.dump()
    把结果写成 JSON

  这是所有 benchmark 共用的底层壳。

  ### /data/zmy/OurAgent/benchmark/framework_runner.py

  作用：这是现在最关键的公共桥。

  关键函数：

  - benchmark_scene_session(scene)
    临时把 benchmark scene 注入 runtime/sandbox session，跑完再恢复
  - benchmark_feature_flags()
    给 benchmark 默认特征开关
    这里默认：
      - 开 sandbox_evaluator
      - 开 semantic_audit
      - 开 checkpoint_repair
      - 关 reflection
      - 关 playbook_write
  - default_env_state(scene)
    从 benchmark scene 推出机器人初始局部状态
  - run_understanding_and_planning(...)
    真正让 benchmark case 走一次 OurAgent 的 understanding + planning 子图

  这个文件的意义非常大：
  它让三篇论文都能共用“把自己的 scene 和任务送进主框架”这条路，而不是每篇论文自己乱接。

  ### /data/zmy/OurAgent/benchmark/run.py

  作用：通用 CLI 入口。

  它现在能识别：

  - alfred
  - wah
  - eai
  - delta
  - reactree_zsp

  它负责：

  - 选 dataset
  - 调对应 adapter
  - 调 BenchmarkRunner
  - 写结果文件

  不过要注意：
  你现在真正重点接的三篇论文里，EAI 目前更常用的是自己的独立 benchmark/eai/runner.py，因为它有 direct/framework 两种模式。

  ———

  # 1. EAI 这条线现在是什么情况

  目录是：

  - /data/zmy/OurAgent/benchmark/eai

  ### /data/zmy/OurAgent/benchmark/eai/adapter.py

  作用：把 EAI 的 prompt cache 样本转成 BenchmarkCase。

  关键内容：

  - EAIActionSequencePrompt
    表示一条 action sequencing prompt
  - load_prompt_cache(prompt_cache_path, id_to_task_path)
    读：
      - helm_prompt.json
      - id2task.json
  - load_valid_case_ids(path)
    读“官方 gold test 可用 case 白名单”
  - to_benchmark_case(prompt, scene_id=1)
    转成 BenchmarkCase
  - EAIBenchmarkAdapter.iter_cases()
    枚举 case，并按 whitelist 过滤掉官方 evaluator 自己不认的坏 case

  现在它已经不是空壳了，是真读：

  - EAI prompt cache
  - task id 映射
  - valid case id 白名单

  ### /data/zmy/OurAgent/benchmark/eai/config.py

  作用：集中管理 EAI 这条线的路径和模型配置。

  关键字段：

  - prompt_cache_path
  - id_to_task_path
  - valid_case_ids_path
  - llm_response_root
  - evaluate_output_root
  - api_base
  - api_key
  - model_name

  关键函数：

  - load_config()

  它现在会默认读取主框架 planning 模型配置，但允许用环境变量覆盖。

  ### /data/zmy/OurAgent/benchmark/eai/translator.py

  作用：这是 EAI 最特别的文件，负责 prompt/scene/动作的双向翻译。

  它现在干三件事：

  1. 从 EAI prompt 里拆出结构信息
      - parse_objects_section()
      - parse_nodes_section()
      - parse_edges_section()
      - parse_node_goals()
      - parse_edge_goals()
      - parse_action_goals()
  2. 把 EAI prompt 还原成 OurAgent 可用 scene
      - build_scene_from_prompt(prompt)
  3. 把 OurAgent 的 todo_list 翻译成 EAI evaluator 能吃的动作 JSON 字符串
      - render_eai_action_sequence(todo_list, object_map, scene)

  还有一个很关键的函数：

  - synthesize_instruction_from_prompt(prompt, fallback_instruction)
    因为 EAI 原始任务有时是泛指，比如 Put groceries in Fridge
    这对理解层太模糊，所以这里会根据 edge/node goal 自动改写成更明确的指令，比如：
      - Open freezer and put food_food inside freezer

  这个文件本质上就是：
  EAI 世界 <-> OurAgent 世界 的翻译器

  ### /data/zmy/OurAgent/benchmark/eai/runner.py

  作用：EAI 的主入口。

  它现在有两种模式：

  #### 1. direct

  直接拿 EAI prompt 去打模型，输出论文 evaluator 要的 llm_output

  关键函数：

  - _generate_outputs(...)
  - run_case(...)

  #### 2. framework

  先走 OurAgent 框架，再翻译回 EAI 动作格式

  关键函数：

  - run_framework_case(case_input)

  这里的流程是：

  - build_scene_from_prompt()
  - synthesize_instruction_from_prompt()
  - run_understanding_and_planning()
  - render_eai_action_sequence()

  也就是说，framework 模式才是真正的：
  EAI benchmark -> OurAgent understanding/planning/sandbox -> EAI evaluator

  其他辅助函数：

  - _sanitize_model_label()
  - _output_path()
  - _isolated_response_root()
  - _isolated_evaluate_root()
  - main()

  隔离目录的设计很重要，因为 EAI 官方 evaluator 会扫描整个输出目录，混入旧文件就会炸。

  ### /data/zmy/OurAgent/benchmark/eai/metrics.py

  作用：调用 EAI 官方 evaluator，并读 summary。

  关键函数：

  - evaluate_action_sequencing_outputs(...)
    现在是通过：
      - /data/zmy/envs/eai-eval/bin/python
        单独跑 official evaluator
  - load_summary(evaluate_output_root, model_label)

  注意：
  EAI 官方 evaluator 现在有一个现实问题：
  它对单 case 或某些小子集不稳，会自己除零，不是我们框架先坏。

  ### /data/zmy/OurAgent/benchmark/eai/valid_virtualhome_action_sequencing_case_ids.json

  作用：EAI 官方 gold test 可用 case 白名单。

  我已经扫过官方数据：

  - 总 342 个 case
  - 官方 gold test 可用 305 个
  - 无效 37 个

  这个文件的意义是：
  先别把官方自己都不认的 case 混进来，不然 benchmark 会脏。

  ### 当前 EAI 做到哪

  已经做到：

  - direct 模式能真实打模型
  - official evaluator 能真实被调起
  - framework 模式简单 case 11_1 Turn on light 已经能输出正确：
      - WALK
      - SWITCHON

  还没完全做完：

  - fridge / 洗衣机 / 多步骤关系任务在 framework 模式下还不稳
  - official evaluator 对单 case/小子集有除零 bug，要继续绕开或包一层

  ———

  # 2. DELTA 这条线现在是什么情况

  目录是：

  - /data/zmy/OurAgent/benchmark/delta

  ### /data/zmy/OurAgent/benchmark/delta/config.py

  作用：定义 DELTA 的 repo 根、四个 domain、默认输出路径。

  关键对象：

  - DeltaBenchmarkConfig

  关键函数：

  - load_config()

  现在默认指向：

  - /data/zmy/DELTA

  ### /data/zmy/OurAgent/benchmark/delta/adapter.py

  作用：把 DELTA 的真实任务定义和 scene graph 装成 BenchmarkCase。

  关键对象：

  - DeltaTaskSpec
    表示一条 DELTA 任务规格

  关键函数：

  - _load_module(module_name, path)
    动态加载 DELTA 仓库里的 Python 文件
  - load_delta_task_specs(repo_root)
    读：
      - data/example.py
      - data/scene_graph.py
        生成四个 domain 的真实任务列表
  - to_benchmark_case(spec)
    转成 BenchmarkCase
  - DeltaBenchmarkAdapter.iter_cases()
    枚举 case，可按 case_id 或 domain 过滤

  现在它已经真实覆盖四个主 domain：

  - pc
  - clean
  - dining
  - office

  ### /data/zmy/OurAgent/benchmark/delta/runner.py

  作用：把 DELTA 的 nested scene graph 转成主框架 scene，并走 one-shot planning。

  关键函数：

  - _delta_states(state)
    把 DELTA 里的状态字符串转成 OurAgent scene state
  - _delta_node(name, item)
    把 DELTA item 转成 OurAgent node
  - build_delta_scene(scene_graph, instruction)
    把 DELTA scene graph 转成 OurAgent scene
  - run_case(case_input)
    调 run_understanding_and_planning()
  - main()

  当前 run_case() 还是第一版，只是：

  - 把 DELTA 任务送进主框架
  - 看能不能出 one-shot 计划
  - 暂时把成功定义成：
      - is_feasible=True
      - 且没 execution_status=failed

  也就是说：
  现在 DELTA 还在“把数据和场景接进来”的阶段，真正论文口径的 success 判定还没完全落完。

  ### /data/zmy/OurAgent/benchmark/delta/metrics.py

  作用：定义 DELTA 的结果聚合。

  关键函数：

  - evaluate_prediction(prediction, reference)
  - summarize_domain_success(results)

  现在已经能按 domain 聚合：

  - 每个 domain 有多少 case
  - success rate 是多少

  ### 当前 DELTA 做到哪

  已经做到：

  - 真实任务定义已装进来
  - 真实 scene graph 已装进来
  - 四个 domain 已覆盖
  - 第一版 runner 能把 DELTA scene 注入主框架

  还没做完：

  - 还没把 success 判定完全对齐 DELTA 论文/evaluator 口径
  - 还没做本地 baseline vs full framework 的正式对比表

  ———

  # 3. ReAcTree / ZSP 这条线现在是什么情况

  目录是：

  - /data/zmy/OurAgent/benchmark/reactree_zsp

  这里我们不复刻 ReAcTree 本身，而是复用：

  - WAH 数据
  - WAH 评测口径
  - OurAgent 一次性输出动作序列

  ### /data/zmy/OurAgent/benchmark/reactree_zsp/config.py

  作用：定义 ReAcTree/ZSP 这条线的 repo 根和 test set 路径。

  关键对象：

  - ReactreeZSPBenchmarkConfig

  关键函数：

  - load_config()

  现在默认 test set 是：

  - /data/zmy/ReAcTree/dataset/wah_nl_test_rev.json

  ### /data/zmy/OurAgent/benchmark/reactree_zsp/adapter.py

  作用：把 WAH 数据装成 BenchmarkCase。

  关键对象：

  - ReactreeZSPTask

  关键函数：

  - load_tasks(path)
    读取 WAH json
  - to_benchmark_case(task, instruction, instruction_idx)
    转成 BenchmarkCase
  - ReactreeZSPBenchmarkAdapter.iter_cases()
    枚举 case

  因为一个 task 可能有多条自然语言指令，所以这里会把同一个任务拆成多个 case。

  ### /data/zmy/OurAgent/benchmark/reactree_zsp/runner.py

  作用：把 WAH init_graph 转成主框架 scene，再走 one-shot planning。

  关键函数：

  - _wah_states(state_list)
    把 WAH 状态列表转成 OurAgent scene state
  - build_wah_scene(init_graph, init_room)
    把 WAH graph 转成 OurAgent scene
  - run_case(case_input)
    调 run_understanding_and_planning()
  - main()

  当前 run_case() 的 success 很粗：

  - is_feasible=True 且不失败，就给 goal_success_rate=1.0
  - subgoal_success_rate=1.0

  这显然只是第一版壳，后面要真正接 WAH evaluator。

  ### /data/zmy/OurAgent/benchmark/reactree_zsp/metrics.py

  作用：定义 GSR / SSR 聚合。

  关键函数：

  - evaluate_prediction(prediction, reference)
  - summarize_gsr_ssr(results)

  现在已经能算：

  - GSR
  - SSR

  但注意：
  现在算的是我们 runner 输出的聚合，不是论文官方 evaluator 真闭环结果。

  ### 当前 ReAcTree/ZSP 做到哪

  已经做到：

  - WAH 数据入口已接上
  - init_graph 已能转成主框架 scene
  - 第一版 one-shot runner 已能走
  - GSR/SSR 聚合壳已经有了

  还没做完：

  - 还没真正接 ReAcTree/WAH evaluator
  - 还没把 one-shot 输出和官方判分彻底闭起来

  ———

  # 4. benchmark 现在到底是怎么工作的

  现在整个思路已经比较清楚了：

  ## 通用思路

  ### A. adapter

  先把论文的原始东西翻译成 OurAgent 看得懂的 BenchmarkCase

  ### B. scene 转换

  如果论文有自己的 scene/graph，就把它转成 OurAgent 当前的 scene session 结构

  ### C. framework runner

  用 /data/zmy/OurAgent/benchmark/framework_runner.py 把 benchmark scene 临时注入 runtime session

  ### D. 主框架 one-shot

  走：

  - understanding
  - planning
  - sandbox evaluator
  - re-trac

  默认不走：

  - reflection
  - ACE 写入

  ### E. translator

  如果论文 evaluator 不吃 OurAgent 原生动作，就把 todo_list 翻译成论文自己的动作格式

  ### F. metrics

  最后调论文 evaluator 或按论文口径聚合结果

  ———

  # 5. 现在最需要你知道的现实状态

  一句话版本：

  - 主框架已经稳了
  - 三篇论文已经开始真实接，不是占位
  - EAI 最领先，已经到“真模型 + 真 evaluator + framework mode 调试”阶段
  - DELTA 和 ReAcTree/ZSP 已经把真实数据和 scene 接进来了，但评测闭环还在继续补

  再直白一点：

  - 现在不是“怎么设计 benchmark”
  - 而是 benchmark 已经开始落地，正在从第一条 EAI 线往后推

  如果你要，我下一条我可以继续给你专门讲：

  1. EAI 现在为什么 11_1 能过、1004_2 还没过
  2. DELTA 接下来具体要补哪些 skill / scene 映射
  3. ReAcTree 的 GSR/SSR 后面怎么接到官方 evaluator